package com.example.demo.tests.data;

public enum DataType {
        STRING_ID,
        STRING_DATA,
        STRING_FIElD,
        LONG,
        INT;
        enum Persons {
                TEACHER,
                STUDENT;
        }
}

